/*Crie uma função que aceite uma string como parâmetro, utilize a função parseInt para converter essa string em um número inteiro e retorne o resultado. */

conversao('123');

function conversao(char) {
    return parseInt(char);
}